services:
  - type: web
    name: facebook-reaction-tool
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 4000
        
